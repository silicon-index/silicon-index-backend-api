{
  "name": "silicon-index-backend-api",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "mcp:serve": "bun run deploy/mcp/server.ts",
    "mcp:dev": "wrangler dev --config deploy/mcp/wrangler.toml",
    "mcp:deploy": "wrangler deploy --config deploy/mcp/wrangler.toml"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "wrangler": "^3.114.0"
  },
  "license": "AGPL-3.0-or-later"
}
